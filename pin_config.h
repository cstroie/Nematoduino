#ifndef pin_config_h
#define pin_config_h

// Transmit and receive pins for distance sensor
const uint8_t pinTrigger = A2;
const uint8_t pinEcho    = A3;

// Pin for status LED (turns on when nose touch neurons stimulated)
const uint8_t statusPin = 13;

#endif
